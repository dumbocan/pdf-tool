{
  "name": "pdf-tool",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Standalone PDF invoice extraction: folder scanning, per-vendor parsers, OCR for scans.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test $(node -e \"const fs=require('node:fs'); process.stdout.write(fs.readdirSync('test').filter((name) => name.endsWith('.test.js') && name !== 'release-artifact-integrity.test.js' && name !== 'engine-stdio.test.js').map((name) => 'test/' + name).join(' '))\") && node --test --test-concurrency=1 test/engine-stdio.test.js",
    "scan": "node bin/pdf-tool.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "canonicalize": "4.0.0",
    "pdfjs-dist": "4.10.38",
    "zod": "4.4.3"
  },
  "bin": {
    "pdf-tool": "bin/pdf-tool.mjs",
    "pdf-tool-engine": "bin/pdf-tool-engine.mjs"
  }
}
